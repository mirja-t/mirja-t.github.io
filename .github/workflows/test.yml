name: Test Frontend

on:
    pull_request:
        branches:
            - master
jobs:
    test:
        runs-on: ubuntu-latest
        container:
            image: node:24
        steps:
            - name: Checkout repository
              uses: actions/checkout@v5
            - name: Install dependencies
              run: npm ci
            - name: Run tests
              run: npm run test
